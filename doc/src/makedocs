#!/bin/csh

#foreach pack ( arc bgo blls bllsb bqp bqpb bsc check clls cro ccqp cqp dgo dps dqp eqp fdc fdh filtrane fisqp glrt gls gltr hash icfs ir lancelot lancelot_simple lpa lpb lsp lsqp l2rt lsrt lstr llsr llst lms miqr mop nls nlpt presolve psls qpa qpb qpc qpp qpt rand roots rpd rqs sbls scale scu sha sils sls slls smt sort symbols trb trs tru ugo uls wcp )
#foreach pack ( filtrane )
#foreach pack ( sils )
#foreach pack ( sort )
#foreach pack ( bllsb )
#foreach pack ( blls slls )
#foreach pack ( slls )
#foreach pack ( bqpb )
#foreach pack ( ccqp )
#foreach pack ( cqp )
#foreach pack ( lpa )
#foreach pack ( lpb )
#foreach pack ( gltr )
#foreach pack ( glrt )
#foreach pack ( lstr )
#foreach pack ( lsrt )
#foreach pack ( l2rt )
#foreach pack ( lsrt )
#foreach pack ( roots )
#foreach pack ( glrt gltr )
#foreach pack ( glrt lsrt gltr lstr l2rt )
#foreach pack ( lstr lsrt l2rt)
#foreach pack ( glrt )
#foreach pack ( galahad )
#foreach pack ( lancelot )
#foreach pack ( lancelot_simple )
#foreach pack ( wcp )
#foreach pack ( eqp )
#foreach pack ( fdc )
#foreach pack ( fdh )
#foreach pack ( dqp )
#foreach pack ( nls )
#foreach pack ( fisqp )
#foreach pack ( ir )
#foreach pack ( qpp )
#foreach pack ( lsp )
#foreach pack ( sha )
#foreach pack ( clls )
#foreach pack ( lsqp )
#foreach pack ( qpa )
#foreach pack ( qpb lsqp )
#foreach pack ( qpb qpc )
#foreach pack ( qpc )
#foreach pack ( cqp dqp )
#foreach pack ( cro )
#foreach pack ( qpc eqp )
#foreach pack ( lsqp )
#foreach pack ( lsqp wcp qpa qpb qpc trs rqs sbls )
#foreach pack ( eqp lsqp qpa qpb qpc qp bqp cqp dqp wcp fdc )
#foreach pack ( bqp )
#foreach pack ( qp )
#foreach pack ( mop )
#foreach pack ( fdc )
#foreach pack ( check )
#foreach pack ( dqp fdh )
#foreach pack ( bsc )
#foreach pack ( rand )
#foreach pack ( lms  )
#foreach pack ( miqr )
#foreach pack ( scale )
#foreach pack ( gls )
#foreach pack ( uls )
#foreach pack ( sls )
foreach pack ( psls )
#foreach pack ( sbls )
#foreach pack ( icfs )
#foreach pack ( smt )
#foreach pack ( trs rqs )
#foreach pack ( dps )
#foreach pack ( trb )
#foreach pack ( gltr scu )
#foreach pack ( trs )
#foreach pack ( llst )
#foreach pack ( llsr )
#foreach pack ( rqs)
#foreach pack ( tru )
#foreach pack ( arc )
#foreach pack ( arc tru trb )
#foreach pack ( arc tru trb ugo bgo dgo nls)
#foreach pack ( rpd )
#foreach pack ( ugo )
#foreach pack ( bgo )
#foreach pack ( dgo )
#foreach pack ( hash )
#foreach pack ( ir )
#foreach pack ( gltr sbls eqp )
#foreach pack ( symbols )
#foreach pack ( lsqp qpb qpa gltr )
#foreach pack ( gltr lsqp presolve qpa qpb qpp qpt scu smt )
#foreach pack ( presolve qpa qpb qpp qpt )
#foreach pack ( presolve )
#foreach pack ( s2qp )
#foreach pack ( qpt cqp dqp )
#foreach pack ( qpa qpb qpc )
#foreach pack ( qpa qpb qpc qpp lsqp fdc symbols eqp sbls )
#foreach pack ( cqp eqp fdc lsqp qpa qpb qpc qp rqs sbls sls trs wcp )
  echo "    " $pack
  my_latex $pack
  my_latex $pack
  my_latex $pack
  psfromdvi ../$pack
# lpr ../$pack.ps
  ps2pdf "-sPAPERSIZE=a4" ../$pack.ps ../$pack.pdf
  rm ../$pack.ps
  cp ../$pack.pdf ../../../galahad_docs/pdf/Fortran/
end
