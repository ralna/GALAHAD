if not libhsl.found()
  if build_single
    galahad_examples += ['ma48ss', files('ma48/ma48ss.f90')]
  endif
  if build_double
    galahad_examples += ['ma48ds', files('ma48/ma48ds.f90')]
  endif
endif
