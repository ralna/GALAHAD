if host_machine.system() == 'windows'
    libgalahad_cc_src += files('regcomp.c',
                               'regex.c',
                               'regexec.c',
                               'regex_internal.c')
endif
